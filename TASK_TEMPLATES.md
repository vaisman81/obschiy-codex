# TASK_TEMPLATES.md

## 1) Диагностика бага
"Найди причину бага в <путь/модуль>. Дай: (1) гипотезу причины, (2) минимальный фикс, (3) проверку фикса командами и ожидаемым результатом. Не делай широкий рефакторинг."

## 2) Правка Python-скрипта
"Измени <file.py> под требование: <требование>. Сохрани совместимость входных/выходных форматов. После правки запусти `python -m py_compile` и короткий smoke-test."

## 3) Правка Node .mjs
"Обнови <file.mjs>: <требование>. Используй ESM, без лишних зависимостей. После правки выполни `node --check` и запуск целевого сценария."

## 4) Правка vm_portal
"Внеси изменения в `vm_portal` для: <требование>. Синхронизируй `public/index.html`, `public/app.js`, `public/styles.css` при необходимости. Проверь ключевые состояния UI: loading/empty/error."

## 5) Инфра/системная операция
"Подготовь и выполни безопасный план для: <операция>. Сначала опиши шаги и риски, затем выполняй. Зафиксируй команды и итоговые артефакты."

## 6) Отчёт по задаче (единый формат)
"Верни результат в формате:
1. Что сделано
2. Почему это решение
3. Как проверено
4. Риски / следующий шаг"
